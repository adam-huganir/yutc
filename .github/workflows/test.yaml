on:
  pull_request:
    branches:
      - main
  push:

jobs:
  go-test:
    name: Go Test
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '^1.22'
      - name: Run tests
        run: go test -v ./cmd/yutc
